import { GetCurrencyCodePipe } from './get-currency-code.pipe';

describe('GetCurrencyCodePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCurrencyCodePipe();
    expect(pipe).toBeTruthy();
  });
});
